// ----------------------------------------------------------------------
import { Shape } from '@mui/system';

const shape: Shape = {
  borderRadius: 8,
  borderRadiusSm: 12,
  borderRadiusMd: 16,
};

export default shape;
